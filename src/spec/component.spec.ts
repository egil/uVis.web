/// <reference path="../.typings/jasmine.d.ts" />

import propertyModule = module('uvis/property');
import componentModule = module('uvis/component');
import up = propertyModule.uvis;
import uc = componentModule.uvis;

export module uvis.spec {
    describe('component:', () => {
        
        describe('instantiating a new component', () => {

        });

        describe('creating content', () => {

        });

    });
}